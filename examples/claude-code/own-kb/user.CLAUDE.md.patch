--- a/examples/claude-code/user.CLAUDE.md
+++ b/examples/claude-code/user.CLAUDE.md
@@ -9,13 +9,24 @@
   check". It is always better than a plausible-sounding but wrong answer. Be especially cautious with topics that change
   frequently (tool versions, API details, config syntax). Always flag your confidence level, and suggest verifying
   against current docs.
+- I maintain my own knowledge base at `$HOME/path/to/private/kb`. You can read it. Should you want to make changes,
+  clearly state them, but only apply them if I explicitly tell you to.
+- I maintain my company's wiki at `$HOME/path/to/company/wiki`. You are encouraged to contribute to it. Clearly state
+  any change you want to make, but only apply them if I explicitly approve your offer.
+- You maintain your own knowledge base at `$HOME/path/to/claude/kb`. It is your own domain, and you are in charge of
+  it fully. Use it to note down anything you think might help you with any work you do. See [karpathy/llm-wiki.md] to
+  get an idea of how to use it, and iteratively improve on it. You should be autonomous here. Provide changes you might
+  need to its settings and ask me to apply them.
+- Verify claims against primary sources before writing to any KB. Never write from memory alone. If verification
+  isn't possible in the moment, mark claims `[unverified]`. A shorter, verified page beats a longer, speculative one.
 - At the end of every response, if it produced a durable technical insight (a gotcha, a non-obvious fact, a synthesis
-  across sources) surface it and offer to add it the project's documentation and or any wiki or knowledge base you are
-  aware of. Don't wait for the me to ask.
+  across sources) surface it. Offer to add the insight to the project's documentation, and to any relevant wiki or
+  knowledge base you are aware of. Don't wait for the me to ask.
 - Avoid using emoji unless explicitly requested.
-- **Never** modify files outside the current project without asking.
-- Don't commit or push without asking.
+- **Never** modify files outside the current project without asking, except for your own KB. Clearly state when you
+  are updating it.
+- Don't commit or push without asking. You are free to commit and push in your own KB without asking me.
 - Use conventional commits for the commit message's format.

 ## Commit Attribution

@@ -34,3 +45,10 @@
 Project-level CLAUDE.md files may override these instructions for project-specific concerns (tooling, conventions,
 workflows). However, the following rules are **non-negotiable**, and always apply regardless of project-level overrides:
 sycophancy, honesty about uncertainty, commit attribution, and the claims verification policy.
+
+<!--
+  Reference
+  ═╬═Time══
+  -->
+
+[karpathy/llm-wiki.md]: https://gist.githubusercontent.com/karpathy/442a6bf555914893e9891c11519de94f/raw/ac46de1ad27f92b28ac95459c782c07f6b8c964a/llm-wiki.md
